'use strict';

const expect = require('chai').expect;
const constructor = require('../lib/bitmap-constructor.js');

describe('File Reading Module', function(err, data) {
  describe('it should err for a empty buffer', function(err, data) {
    it('should return an error for empty buffer', function(){
       // this will check buffer is empty before load
    });
  });
  describe('it should check that read buffer is the same as our buffer object', function(err, data){
    it('should be the same as our object', function() {
      // read the buffer and compare to our object
    })
  })
});
